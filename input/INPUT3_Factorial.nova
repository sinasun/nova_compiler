package main

import (
    "fmt"
)

func main() {
    var i, n, f int

    f = 1
    i = 1

    fmt.Print("Write a number: ")
    fmt.Scan(&n)

    for i < n {
        f = f * i
        i = i + 1
    }

    fmt.Println("Factorial:")
    fmt.Println(f)
}
